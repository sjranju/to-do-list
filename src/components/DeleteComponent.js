import React from 'react'

const DeleteComponent = (props) => {
    const check={props}
  return (
    <div>
        if(check.length)
        <h2>Ticked items <br /></h2>
    </div>
  )
}

export default DeleteComponent
